apiVersion: "v1"
kind: "Service"
metadata:
  name: "mongo-svc"
  namespace: "final"
  labels:
    app: "mongo-db"
spec:
  ports:
    - name: "mongodb"
      port: 27020
      targetPort: 27017
      nodePort: 30020
  type: NodePort
  selector:
    app: "mongo-db"
